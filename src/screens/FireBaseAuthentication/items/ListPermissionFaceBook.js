import React from 'react';
import {View} from 'react-native';
import {SIZE} from '../../../utils';

export default function ListPermissionFaceBook() {
  return (
    <View
      style={{
        height: SIZE.height(20),
        width: SIZE.width(100),
        backgroundColor: 'red',
      }}></View>
  );
}
